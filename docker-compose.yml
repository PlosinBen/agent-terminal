services:
  dev:
    build: .
    volumes:
      - .:/app
      - node_modules:/app/node_modules
    stdin_open: true
    tty: true
    working_dir: /app
    command: npm run dev

volumes:
  node_modules:
